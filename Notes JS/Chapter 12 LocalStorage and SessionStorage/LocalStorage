 What is LocalStorage
 Ans = LocalStorage is a way to store data on the client’s computer. It allows the saving of key/value pairs in a web browser and it stores data with no expiration date. LocalStorage can only be accessed via JavaScript, and HTML5.

 1)Data Stored Locally
 2)Data Never Expires
 3) Methods = setItem(), getItem(), removeItem(), clear()
